# Implementation Plans

Round 1 generated by the `improve next` (direction) skill on 2026-06-30, against commit
`4948270`. Round 2 (plans 005–009) generated 2026-07-12, against commit `212ba7c`.
These are **direction / spike plans** — each scopes a piece of forward work, and two of
them (002, 003) are explicitly designed to return a GO/NO-GO before any larger build.

> **⚠ Round-2 reconcile finding (2026-07-12):** all four round-1 plans are DONE and
> APPROVED but **none of their branches ever merged to main** — the gate harness,
> weather verdict, `export-tips`, and `monitor` all sit on unmerged branches. Plan 005
> lands them; it precedes everything else in round 2.

Each executor: read the plan fully before starting, honor its STOP conditions, and update
your row below when done. All four plans are **read-only on `src/engine/`** except 003/004,
which add CLI/analysis code but never touch the model.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | OD-split (v5) October promotion-gate harness | P1 | M | — | DONE 2026-09-05; merged in PR #60 |
| 002a | Audit/enrich weather data upstream (AFL-MCP / fitzRoy-ts) | P2 | S–M | — | DONE ✅ (APPROVED; NO-ENRICH; 100% AFLM weather coverage 2010–2025; findings doc in AFL-MCP/docs/weather-coverage-audit.md) |
| 002b | Pre-registered univariate test of weather signal (tipper) | P2 | S–M | 002a | DONE 2026-09-05; merged in PR #60 |
| 003 | Spike Squiggle submission channel + tip-export command | P2 | S–M | — | DONE 2026-09-05; merged in PR #60 |
| 004 | Port the weekly comp monitor's scoring core into the typed CLI | P3 | M | — | DONE 2026-09-05; merged in PR #60 |
| 005 | Land the four approved advisor branches onto main | P1 | S | — | DONE 2026-09-05; four commits landed in PR #60 |
| 006 | Pre-register A2 October bundle + one-button eval harness | P1 | M | 005 | SUPERSEDED by Task 40 campaign; historical research closed |
| 007 | 2027 comp entry: gameid resolver, tips-endpoint skeleton, contact checklist | P2 | M | 005 | DONE 2026-09-05; PR #64 merged, contact and submission await Jack |
| 008 | Travel-distance/timezone univariate probe (D5 resurrection) | P3 | S–M | — | SUPERSEDED by Task 40 campaign; historical research closed |
| 009 | Weekly comp monitor as GitHub Actions cron | P3 | S | 005 | DONE 2026-09-05; merged in PR #65 |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended order & dependency notes

- **001 first** — it builds the missing gate for the project's only live model candidate
  (OD-split / v5), whose promotion decision is scheduled for October 2026. Highest leverage.
- **002a → 002b** — the weather work is split: 002a (upstream, in the afl-stats repo)
  audits and, if warranted, enriches the weather data, then **gates** 002b (the tipper-side
  signal test). Do not run 002b until 002a reports the coverage it can rely on. This mirrors
  the DOB precedent (fixed upstream in afl-stats, then consumed in tipper).
- **003** is an independent spike; run any time. It ends with an explicit GO/NO-GO that
  decides whether a larger submission build gets written.
- **004 last** — pure hygiene (no modelling gain), and it composes best *after* 001: 001's
  field-scoring (Python) and 004's monitor scoring (TS) should converge on one TS module.
  If 001 lands first, 004 lifts its consensus logic into `src/cli/monitor/`.
- **Soft coupling 001 ↔ 004**: both score models against the Squiggle field. No hard
  dependency, but doing 001 then 004 avoids two divergent copies of the scoring conventions.

## Weather split (maintainer decision, 2026-06-30)

The maintainer asked whether weather is better addressed in the upstream `fitzroy`/afl-stats
pipeline than in tipper. Resolved by splitting the original plan 002:

- **002a** (afl-stats / fitzroy, upstream): weather *data coverage and quality* is an
  upstream concern — audit what the source exposes and enrich D1 if it's richer than what's
  stored. **Note: 002a executes in the afl-stats repo, which is not in this session**, so it
  is written investigation-led with STOP conditions.
- **002b** (tipper): the *signal test* — "does weather explain v3's residuals?" — needs v3's
  predictions and therefore stays in tipper. Gated on 002a's coverage report.

## Round 2 (2026-07-12) order & dependency notes

- **005 first, blocking** — 006, 007, 009 all consume code that only exists on the
  unmerged round-1 branches. 008 is the one round-2 plan that can run before 005.
- **006 before ~mid-September** — its whole value is pre-registering the October bars
  before full-2026 results exist. It reuses 001's `od-consensus-gate.py` for gate 4.
- **007 timing is external**: Squiggle contact + soak period should happen during the
  2026 season; the code half is independent of the calendar.
- **008 anytime** — resurrection trigger fired (002b NO-GO). Honest prior: another
  documented negative; that outcome still closes the new-information ledger.
- **009 after 005**; its weekly log also feeds 006's gate-2 trend line, so sooner is
  mildly better.

## Findings considered and NOT turned into plans

- **D5 — graded travel-distance / timezone feature**: ~~deferred round 1~~ → **promoted
  to plan 008 in round 2** (its written trigger — weather NO-GO — fired).
- **Config store archival (156 configs in `configs/`)**: research detritus, a tech-debt item
  rather than a direction one — out of scope for `next`. Worth a separate `improve quick
  tech-debt` pass if it starts slowing config operations.

## Provenance

- Round 1 audit scope: **direction category only** (the `next` variant). Correctness,
  security, tests, performance, and dependency audits were **not** run that session.
- Round 1 sources: `HANDOFF.md`, `docs/task-38-wheelo-closure-roadmap.md`,
  `docs/task-38b-od-preflight.md`, `README.md`, `CLAUDE.md`, and the `analysis/` scripts.
- Round 2 (2026-07-12, commit `212ba7c`): **direction only** again. Sources: round-1
  plan branches (diffs reviewed directly), `docs/task-38b-od-preflight.md`,
  `docs/task-39-squiggle-submission.md` (on the 003 branch),
  `analysis/od-consensus-gate-verdict.md` (on the 001 branch), `HANDOFF.md`,
  `src/engine/metrics.ts`, `.github/workflows/ci.yml`, `docs/task-26-rest-travel.md`.
  Correctness/security/tests/perf/deps still un-audited across both rounds.

## Task 41 addendum, 2026-09-05

The ship-mode prompt supersedes plan 005's stale base and merge instructions.
Four single commits were cherry-picked, preserving the Worker. The two CLI
registration conflicts retain publish, export-tips, and monitor. Publication
awaits explicit approval after automatic review rejected the GitHub push.

## Task 41 Completion, 2026-09-05

Jack authorised GitHub publishing. PR #60 landed the four adviser commits.
PR #64 added the competition feed and PR #65 added the scheduled monitor.
The final report records the release and open sibling PRs. Squiggle contact
and submissions still require Jack's separate instruction.
