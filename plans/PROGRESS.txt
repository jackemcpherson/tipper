[Review] 2026-05-01 05:02 UTC - release (blocking)

### Verdict: PASSED

### Findings

(No issues found)

---

[Review] 2026-04-30 10:15 UTC - release (blocking)

### Verdict: NEEDS_WORK

### Findings

1. **RL-001**: Missing Changelog - No CHANGELOG.md exists
   - File: CHANGELOG.md:0
   - Issue: CHANGELOG.md does not exist. This is a significant release (v2 model with corrected PAV, new bootstrap infrastructure, contextual K and venue HA engine support). The changes need to be documented for the release.
   - Suggestion: Create CHANGELOG.md with an entry for v2.0.0 documenting: PAV defence bug fix (Task 13), PAV recovery and re-calibration (Task 15), bootstrap comparison infrastructure, contextual K-factor engine (Task 16), venue HA engine (Task 17), and v2 validation results (Task 18).

2. **RL-002**: Dirty Working Directory - 111 uncommitted changes
   - File: .:0
   - Issue: Working directory has 111 uncommitted changes including 14 modified source files and ~97 new files (configs, docs, analysis, tests). These must be committed before tagging a release.
   - Suggestion: Stage and commit all changes in a logical commit (or series of commits). Recommended: one commit for engine changes + tests, one for configs + docs.

3. **RL-003**: Version Mismatch - package.json version does not reflect v2 release
   - File: package.json:2
   - Issue: package.json has version "0.1.0" but this is a v2 model release with significant architectural changes (PAV restoration, new engine features, new CLI commands). The version should be bumped.
   - Suggestion: Update package.json version to "2.0.0" to reflect the v2 model baseline. Also update the matching version in src/cli/index.ts:12.

4. **RL-004**: Missing Documentation - No README.md exists
   - File: README.md:0
   - Issue: No README.md file exists in the repository. A release should have basic project documentation.
   - Suggestion: Create a README.md with: project description, setup instructions (bun install, wrangler dev), CLI usage (backtest, predict, compare, config commands), model card for v2 (from Task 18 report), and link to CLAUDE.md for developer guidance.

5. **RL-005**: Version Mismatch - CLI version string differs from release version
   - File: src/cli/index.ts:12
   - Issue: CLI reports version "0.1.0" via `.version("0.1.0")` which will not match the release version.
   - Suggestion: Update to `.version("2.0.0")` to match package.json after the version bump.

---

[Review] 2026-06-13 03:54 UTC - repo-structure (warning)

### Verdict: NEEDS_WORK

### Findings

1. **RS-001**: Missing Env Template - .env.example missing while env vars are used
   - File: .env.example:0
   - Issue: The README documents four environment variables (CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_D1_DATABASE_ID, TIPPER_NO_CACHE) and src/cli/db.ts + src/cli/cache.ts read them at runtime, but no `.env.example` template exists at the repo root. New contributors (or future-you on a fresh clone) get no scaffold for which vars to set.
   - Suggestion: Add a `.env.example` at the repo root listing each variable from the README's env-vars table with placeholder values and a one-line comment for each.

2. **RS-002**: Incomplete README - README's "Current model" section is stale (v2 vs. actual v3)
   - File: README.md:88
   - Issue: The README's "Current model (v2)" block advertises `pavfix-blend-w06` with LogLoss 0.8607 and 2026 OOS 0.8029, but the project's actual current pointer is `predha-080` (v3) with LogLoss 0.8485 (2021–25) / 0.7925 (2026) per CLAUDE.md, CHANGELOG.md, and configs/_current.json. The README's `tipper config promote` example also references v2. A reader following the README would set wrong expectations for performance and run examples against the wrong config.
   - Suggestion: Update the "Current model" block to v3 (`predha-080`, K=25, HA=160, RTM=0.10, sigma=36, weight_elo=0.6, slope=6.986, prediction_home_advantage=80) with the v3 metrics, and refresh the `promote` example to use `predha-080`. Add a `Tasks 19–37` link to docs/ alongside the existing "Tasks 1–18" reference.

3. **RS-003**: Incomplete README - no contribution guidelines or CONTRIBUTING.md link
   - File: README.md:0
   - Issue: README has no contribution section. The project header explicitly says "This is personal tooling: it requires your own Cloudflare D1 database populated with the afl-stats schema. It will not work out of the box against someone else's data." — i.e. contribution is not invited. Surfacing that intent explicitly satisfies the "guidelines OR link" rule without inviting drive-by PRs the maintainer doesn't want.
   - Suggestion: Add a one-paragraph "Contributing" section near the bottom that states the project is personal tooling, that issues and PRs from outside the maintainer are out of scope, and points to docs/ for the research ledger.

---

[Review] 2026-06-13 03:55 UTC - release (blocking)

### Verdict: NEEDS_WORK

### Findings

1. **RL-001**: Missing Changelog - No CHANGELOG entry for work since 3.3.1 (T33–T37 + A3 monitor)
   - File: CHANGELOG.md:1
   - Issue: The most recent CHANGELOG entry is `## [3.3.1] - 2026-06-12` covering the v4 revert (Task 32). Since then this branch adds Tasks 33, 34, 35, 36, 37, the A3 weekly comp monitor (`analysis/weekly-monitor.py` + `weekly-monitor-log.csv`), the DOB-backfill mention (work in AFL-MCP), and new engine machinery shipped inert (OD split ratings in `src/engine/odelo.ts`, age-curve helpers in `src/engine/prior.ts`, DOB plumbing through `HarnessData`/orchestration, new `pav.age_curve_weight` and `elo.od.*` optional schema fields). None of this is documented in the changelog.
   - Suggestion: Add a `## [3.4.0] - 2026-06-13` entry summarising: A3 weekly monitor + market alert; T36 OD split ratings (parked, machinery inert); T37 age-curve PAV priors (killed, machinery inert); T33/T34/T35 research outcomes; DOB backfill (in AFL-MCP) unblocking T30; plus the new optional schema fields. Use the existing 3.3.1/3.2.0 entry style (model section + research bundle).

2. **RL-002**: Version Mismatch - package.json at 3.3.1 but this PR includes T33–T37 + A3 worth a MINOR bump
   - File: package.json:3
   - Issue: package.json is at `"version": "3.3.1"` (set by commit 33f3e47 for the v4 revert) but the 3.3.1 work has never been tagged — there is no `v3.3.1` tag on the remote (existing tags: v2.0.0, v3.0.0, v3.0.1, v3.1.0, v3.1.1, v3.2.0). The PR adds two new optional schema fields (`elo.od` and `pav.age_curve_weight`), a new CLI-adjacent artifact (`analysis/weekly-monitor.py`), and two new engine modules (`odelo.ts`, plus age-curve helpers in `prior.ts`). Under semver this is a MINOR bump, and bundling T31–T37 into one tag also keeps the public release history clean (3.2.0 → 3.4.0, skipping the never-released 3.3.x stretch).
   - Suggestion: Bump package.json to `"version": "3.4.0"` and align the new CHANGELOG entry (from RL-001) to `## [3.4.0] - 2026-06-13`. The release workflow (`.github/workflows/release.yml`) keys on the `v*` tag and runs typecheck/lint/test/build + npm publish via OIDC, so after merge the release is `git tag v3.4.0 && git push origin v3.4.0`.

3. **RL-003**: Dirty Working Directory - plans/PROGRESS.txt has unstaged changes
   - File: plans/PROGRESS.txt:0
   - Issue: `git status --porcelain` shows ` M plans/PROGRESS.txt` from this and the prior reviewer appending findings. The release-reviewer standard requires a clean working tree before tagging.
   - Suggestion: After applying RL-001/RL-002 fixes (and the repo-structure RS-001/RS-002/RS-003 fixes), commit the bundle including the updated PROGRESS.txt. No separate action needed beyond making sure the fix commit includes it.

4. **RL-004**: Untagged TODO - none found, but flagging for completeness
   - File: src/:0
   - Issue: `grep -rn "TODO\|FIXME"` against `src/` and `tests/` came back empty — no untagged TODOs to block release.
   - Suggestion: No action.

---
