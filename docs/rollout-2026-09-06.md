# Production Deployment, 6 September 2026

Verification started with the additive migration and source refresh.

## Deployment Record

AFL-MCP revision `48a909e3822fbc2bdda5a732c25b0f5dbbf58e80` applied migration
`0021_tipper_publication.sql` and passed its production verification in
[apply run 33998827286](https://github.com/jackemcpherson/cloudflare-infra/actions/runs/33998827286).
The source refresh returned successfully. All 2 upcoming AFLM and 77 upcoming
AFLW fixtures had canonical UTC kickoffs. Today's five AFLW matches each had
21 non-emergency players per team in validated observations.

Tipper activation is `2026-09-05T23:32:55.293Z`, or 09:32:55 AEST on 6 September.
The initial deployed revision was `c521235aa7a233e0401dde4b57f36f5ddedf5941`.
The private local file
`~/.config/cloudflare-infra/tipper_admin_token` and the Worker secret
`ADMIN_TOKEN` hold the admin credential outside Git. Its value is not part of
this record.

The first three admitted runs published all 11 eligible matches. AFLM round 27
had 2 provisional tips. AFLW round 4 had 5 confirmed tips, and round 5 had
4 provisional tips. Database checks found zero broken capture/projection links.
The database admitted every capture before its canonical kickoff.

The final Tipper revision is `da587769fdd23c7f084fa4e436b628a3e91eecfc`.
It passed production verification in
[apply run 33999561791](https://github.com/jackemcpherson/cloudflare-infra/actions/runs/33999561791).
The feed remained unchanged after this deployment while its captures still
identified the initial revision. The reporting correction passed 28 tests.
The deployment-policy correction passed 59 infrastructure CLI tests. Both fixes
passed hosted checks before promotion.

## Live Checks

The first scheduler heartbeat was `2026-09-05T23:45:36.704Z`. Health reported
11 expected tips, no missing or stale predictions, no unknown kickoff times and
no missing mappings. Both AFLM feed entries matched every issued database value,
including winner orientation and rounding. Invalid query parameters returned 400,
an unknown round returned 404, and the CORS OPTIONS request returned 204
with no caching.

Authenticated refresh returned publication counts of 2, 5 and 4 for those rounds.
Refreshing completed AFLM round 26 returned zero. An authenticated request with
`forceUnlock` returned 400. An invalid token returned 401 and left the publication
attempt count unchanged at four.

Live Squiggle data validated against the field schema: 6580 tips, including
214 Punters tips. The upcoming AFLM identities are game 38725, Fremantle versus
Geelong, and game 38726, Brisbane Lions versus Adelaide.

| Production workload           | AFLM | AFLW |
| ----------------------------- | ---: | ---: |
| Completed historical matches  | 1437 |  544 |
| Current-season player rows    | 9798 | 1302 |
| Previous-season player priors |  668 |  513 |

All workloads fit the configured snapshot bounds. Cloudflare's live execution
traces recorded 115 ms CPU and 247 ms wall time for AFLM publication. The two
AFLW publications used 50/31 ms CPU and 140/118 ms wall time. All returned `ok`
without exceptions. AFL-MCP source refresh used 273 ms CPU and 5485 ms wall time.

Both Workers report the `standard` usage model with no custom CPU override.
Cloudflare documents a default 30-second HTTP CPU budget and a 30-second budget
for scheduled executions more frequent than hourly. Isolate memory is 128 MB.
See the
[platform limits](https://developers.cloudflare.com/workers/platform/limits/).
These traces prove successful measured workloads, not a production peak-memory
measurement. Local memory measurements remain in [verification](verification.md).

## Observation Gates

The first captured kickoff is AFLW match 1171251 at `2026-09-06T02:30:00Z`,
or 12:30 AEST. After that instant, verify its existing capture remains unchanged
when round 4 refreshes. Native D1 tests already cover crossed deadlines, permanent
locks, reschedules, stale overlapping runs and transaction rollback.

The first legitimate report is due `2026-09-07T22:00:00Z`, or 08:00 AEST on
8 September. It should be an empty prospective AFLM report because the first
captured AFLM matches are on 11 and 12 September. Empty reports have null metrics
and no ranking. The first report with those results is due 14 September at
22:00 UTC, subject to completed source results and available field evidence.

The existing weekly GitHub check runs Monday at 23:30 UTC, downloads the retained
report and attaches it before checking freshness, coverage and alerts. These
observation gates remain open until their actual reporting times.

Squiggle acceptance and a successful ingestion test remain external gates.
Jack can send the [submission packet](submission.md) after the observation
checks. Recheck published 2027 team sizes before that season starts.
